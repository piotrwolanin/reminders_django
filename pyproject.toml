[tool.poetry]
name = "reminders"
version = "0.1.0"
description = ""
authors = ["piotrwolanin <peterjwolanin@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
Django = "^4.2.6"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
